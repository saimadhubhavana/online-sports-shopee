import React from 'react';
import { connect } from 'react-redux';

const deleteUser = () => {

    return (
        <div>
            Delete Page 
        </div>
    )
};

export default connect()(deleteUser);